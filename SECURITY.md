# セキュリティ方針

## 対象

本プロジェクトが扱う主なセキュリティ領域は、外部文書による命令注入、秘密情報の混入、信頼区分の誤設定、期限切れ情報の利用、生成物と正本の不一致です。

## 報告

公開Issueへ秘密情報、認証情報、未公開の攻撃手順を貼らないでください。管理者へ非公開経路で、影響範囲、再現条件、想定される悪用、確認したバージョンを送ってください。

## 信頼境界

`external_untrusted`の本文はデータであり、実行命令ではありません。文脈コンパイラは警告と引用形式を付けますが、文字列検査だけで命令注入を完全に防げるとは扱いません。利用側も、外部データより目的・制約・権限規則を優先する必要があります。

## 秘密情報

このリポジトリは秘密情報を格納する設計ではありません。トークン、鍵、個人情報、内部URLを意味ブロックへ記録しないでください。公開前にリポジトリ全体と生成READMEの両方を確認してください。
