「作らないもの（明示的に範囲外）」へ回答しました。

## 区別を明確にしました

「生成機能が範囲外」と「説明しない」を混同していました。指摘の通り、概念・使い方・引き継ぎ資料は v1 でも必須です。

**2つの新規ブロックを追加:**

### 1. `018-scope-explanation-vs-generation`

| 対象 | v1 の位置付け |
|---|---|
| 各ハーネス要素の**自動生成機能** | 範囲外（次段階） |
| 各ハーネス要素の**概念・使い方の説明** | 範囲内 |
| 権限設定の**自動生成機能** | 範囲外 |
| 権限スコープ・モードの**解説** | 範囲内 |

「範囲外」とだけ書くと「Claude Code の何も分かっていない」と誤解させるため、区別を明示しました。

### 2. `090-guide-primitives`

Claude Code の7プリミティブ（CLAUDE.md/Skills/Hooks/Subagents/MCP/Plugins/権限）について、**概念・使い方・最小構成**を整理した引き継ぎ資料。各要素に：

- **概念:** 何をするものか
- **使い方:** ファイル配置先
- **最小構成:** コード例
- **このプロジェクトとの関係:** v1 で何を扱い、次段階で何を生成するか

PR 本文の「作らないもの」セクションにも注記を入れ、説明は提供することを明示しました。ベンチマーク PASS、CI は次 tick で確認します。
